// app/utils/analytics.ts

export const trackSessionPlay = (userId: string, programId: number, sessionId: number) => {
    // Replace this with your actual analytics implementation
    console.log(`User ${userId} played session ${sessionId} in program ${programId}`);
  };